(include_subdirs unqualified)

(library
  (name motley)	
  (libraries core shell z3 petr4 ANSITerminal extlib)
  (virtual_deps menhir)
  (inline_tests)
  (preprocess (pps ppx_jane ppx_inline_test ppx_deriving_yojson))
  (ocamlc_flags -custom -cclib -lstdc++)
  (ocamlopt_flags -cclib -lstdc++))